# Mobile screenshots

Generate three mobile captures after running `npm run dev`:
1. Landing page hero + quick deploy form.
2. Deploy page with env var table + sticky status rail after kicking off a build.
3. Status page showing logs + QR code.

Save them in this folder as `landing.png`, `deploy.png`, and `status.png`.
